# TV Feed 安全政策

最后更新：2026 年 8 月 10 日

感谢你帮助改进 TV Feed 的安全性。本文件说明当前支持范围、私下报告方式、项目安全边界，以及哪些问题适合按安全漏洞处理。

## 1. 支持的版本

| 版本 | 安全支持 |
| --- | --- |
| 当前默认分支 | 支持；在首个公开版本发布前，这是唯一受支持目标 |
| 最新 GitHub Release | 支持；首个公开版本发布后适用 |
| 更早的 Release、分支或第三方修改版 | 不支持 |

安全修复通常只进入当前默认分支和最新公开版本。报告时请提供 TV Feed 版本、操作系统、CPU 架构和安装来源。

## 2. 私下报告安全问题

首选渠道是本仓库 GitHub 页面中的 **Security → Advisories → Report a vulnerability**。维护者应在公开发布仓库前启用 GitHub Private Vulnerability Reporting；该功能启用后，报告内容只对报告者和仓库安全协作者可见。

项目目前没有专用安全邮箱。如果仓库没有显示“Report a vulnerability”按钮，请只创建一个标题为 `[Security contact request]` 的公开 Issue，请求维护者提供私下沟通方式。**不要在该 Issue 中披露漏洞细节、利用步骤、凭据、私人 Stream Token、未公开 URL 或个人资料。**

请在私下报告中尽量提供：

- 受影响版本、平台和安装来源；
- 可观察到的安全影响及其现实攻击条件；
- 最小、可重复的验证步骤；
- 已脱敏的日志、截图或测试样例；
- 你认为需要临时缓解的风险；
- 是否计划公开披露以及期望的协调时间。

频道、直播链接、Logo、名称或商标的权利移除不属于安全漏洞，请按照 [LEGAL.md](LEGAL.md) 和“权利移除申请”模板处理。

## 3. 响应目标

以下是尽力而为的处理目标，不是服务等级协议：

| 阶段 | 目标时间 |
| --- | --- |
| 确认收到 | 3 个工作日内 |
| 初步分类与影响判断 | 7 个日历日内 |
| 仍在处理时的状态更新 | 至少每 14 个日历日一次 |
| 修复与协调披露 | 根据严重程度、复现条件和发布能力与报告者协商 |

维护者会尽量在修复或缓解措施可用后再公开细节，并在适当情况下发布 GitHub Security Advisory。

## 4. 系统和范围

TV Feed 是本地运行的 Electron IPTV 目录阅读器和播放器。安全政策覆盖：

- 主进程、预加载脚本、渲染端和自定义 `tvfeed://` 协议；
- 目录获取、解析、过滤、本机缓存和项目 denylist；
- 频道搜索、收藏、最近观看、远程台标开关和数据清除功能；
- HLS 播放器与多线路切换；
- 官方构建和打包配置。

TV Feed 不运营代理直播或接收播放内容的项目服务器。iptv-org、频道方、Logo 主机、直播源站、CDN、Electron 及其他依赖属于外部系统；但 TV Feed 处理这些外部输入的方式仍在本项目安全评估范围内。

## 5. 威胁模型和信任边界

以下输入均视为不可信：

- iptv-org API 返回的频道、国家、分类、Logo、blocklist 和直播 URL 数据；
- 本机已有的目录缓存；
- 远程 Logo 图片及其响应；
- HLS 主播放列表、子播放列表、媒体分片、重定向和错误响应；
- 第三方主机名、路径、查询参数和内容类型。

主要保护对象包括用户本机文件与应用数据、Electron 主进程能力、IPC 边界、本机与私有网络、用户凭据或 Token，以及用户对第三方网络请求的知情选择。

## 6. 必须保持的安全属性

以下属性属于项目安全边界，破坏它们通常应作为安全问题报告：

- 渲染进程保持 `sandbox: true`、`contextIsolation: true`、`nodeIntegration: false`、`webSecurity: true`，并禁止不安全混合内容；
- 权限请求默认拒绝，未知窗口打开和离开受信任页面的导航默认拒绝；
- IPC 只接受受信任的 TV Feed 渲染页面，并只暴露最小、固定的方法；
- 自定义协议只能读取打包后的渲染资源，不能通过路径穿越读取任意本机文件；
- 顶层直播 URL 必须是 HTTPS HLS，并拒绝 URL 凭据、本机或私网目标，以及要求自定义 Referer/User-Agent 的线路；HLS 子播放列表、分片、初始化片段、密钥和每次重定向也必须经过相同的 DNS 公网校验与资源上限；
- 播放失败信息必须映射为固定诊断类别，不得在界面、Toast 或普通应用日志中回显完整直播 URL、查询参数、私人 Token 或未经清理的上游错误正文；
- 远程台标默认关闭，只有用户主动开启后才允许发出 Logo 请求，关闭后不再创建新的远程 Logo 请求；
- 收藏、最近观看、上次频道和台标偏好只保存在本机，不上传到 TV Feed 服务；
- “清除目录缓存”和“清除收藏与观看记录”必须删除其声明范围内的持久化数据；
- 家庭安全模式启用后必须关闭远程台标、取消 Logo 请求、清除旧目录缓存与最近观看，并在主进程返回目录前应用本地频道允许列表；
- `is_nsfw` 缺失或不严格等于 `false` 时必须 fail closed；上游 blocklist 和项目 denylist 必须同时作用于新目录与旧缓存；
- 项目不得绕过 DRM、登录、订阅、Token、Cookie、Referer、设备授权或地域限制。

## 7. 适合报告的问题

示例包括但不限于：

- 不可信目录、Logo、HLS 数据或错误消息导致脚本执行、HTML 注入、原生代码执行或沙箱逃逸；
- 绕过 IPC 发送方检查、自定义协议路径限制、导航限制或默认拒绝权限；
- 通过远程 URL、重定向、HLS 清单或媒体子资源访问本机文件、回环地址、私有网络或云元数据服务；
- 在用户启用远程台标前，或用户关闭后，仍然发出可避免的 Logo 请求；
- 清除操作未删除声明范围内的数据，或本地记录被意外上传；
- 私人 Stream Token、凭据、未公开 URL 或其他敏感数据被写入公开日志、崩溃信息或意外的第三方请求；
- 通过缓存、数据类型异常或匹配错误绕过 fail-closed、上游 blocklist 或项目 denylist；
- 第三方依赖漏洞能够通过 TV Feed 的实际代码路径触发，并对用户造成具体安全影响。

严重程度会结合可达性、是否需要用户操作、影响用户数量，以及是否能执行代码、读取本机数据、访问私有网络或泄露敏感信息判断。仅有依赖版本匹配而没有 TV Feed 可达路径，不自动视为可利用漏洞。

## 8. 已知限制

- 成人内容过滤依赖上游元数据和 URL 规则，不分析直播画面或声音；上游误标和实时内容变化不是安全控制失效的充分证据，但应用自身未按声明 fail closed 仍可报告。
- HLS 清单可以引用其他主机、重定向、子播放列表、媒体分片和密钥；这些派生请求仍会逐次经过主进程的安全网络策略。若发现任何派生请求绕过 DNS 公网校验、重定向复检或资源上限，请私下报告。
- 播放和图片加载会把用户 IP、User-Agent、请求路径与时间暴露给相应第三方。远程台标为选择性功能，直播播放本身必须连接源站。
- Chromium、操作系统和第三方 CDN 可能产生正常的临时缓冲或网络缓存；TV Feed 不建立持久媒体库。
- `npm run pack:mac` 生成的是仅作本机验证的 ad-hoc 签名开发包，没有 Apple Developer ID 发布者身份，也未经过 Apple 公证，不应被描述为正式可信发行版。

## 9. 不属于安全漏洞的事项

以下事项通常不按安全漏洞处理，除非同时证明存在 TV Feed 自身的可利用安全影响：

- 频道失效、卡顿、无字幕、地域不可用或节目内容变化；
- 上游 NSFW 标记、频道分类、名称、Logo 或权利状态错误；
- 版权、商标、转播授权或权利移除争议；
- 只影响已停止支持版本或第三方修改版的问题；
- 仅存在于 iptv-org、电视台、CDN 或其他外部系统，且不涉及 TV Feed 处理方式的问题；
- 没有实际可达路径的通用依赖版本告警。

## 10. 不接受的功能和研究行为

项目不会接受用于以下目的的 Issue、Pull Request 或安全报告：

- 绕过 DRM、付费墙、登录、订阅、设备授权或地域限制；
- 窃取、生成、刷新、分享或隐藏使用私人 Token、Cookie 或凭据；
- 伪造请求头以访问未授权直播；
- 扫描、压测、干扰或攻击不属于报告者的 iptv-org、电视台、Logo 主机、直播源站或 CDN；
- 获取、保存或传播并非验证漏洞所必需的第三方内容或个人数据。

请只在你拥有或明确获准测试的环境中研究。此政策不授权对任何第三方基础设施进行安全测试。

## 11. 协调披露与隐私

维护者会把未公开报告限制在修复所需人员范围内，并尽量删除不必要的 Token、凭据和个人资料。报告者也应避免收集或提交与漏洞无关的数据。

在修复、缓解或风险告知准备完成前，请不要公开利用细节。项目会尽量与报告者协调披露时间并在报告者同意时给予致谢，但不能承诺奖励、漏洞赏金或固定修复期限。
